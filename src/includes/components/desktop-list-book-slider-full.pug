mixin list-video(title, dataListBook)
  .list-video.style-section
    .list-video__title.pb-1.d-flex.align-items-center
      h3.d-inline-block.mb-0=title
      a(href="#") (Xem tất cả)
    .list-video__box.pt-4.p-2#list-book__box.owl-carousel
      if (dataListBook)
        each item in [...dataListBook, ...dataListBook]
          .item 
            include book-detail.pug 
            +BookDetail(item)

      else 
        each item in [1,2,3,4,5,6]
          .item 
